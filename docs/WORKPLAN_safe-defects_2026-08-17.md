# safe 결함 74건 수정 캠페인 — 작업계획 (정본)

> 2026-08-17 · 근거 `LEDGER_safe-defects.md`(74건) · 우선순위 `BASELINE_safe-menu-coverage.md`(메뉴 41화면)
> 운영자 지시: **건별 지시서로 쪼개지 말고 한 캠페인으로.** (예외: 51·51-b, ① ㉮ 는 이미 지시서 발행)
> 이 문서가 캠페인 정본이다. 실행하며 확인된 **소유·게이팅**을 반영했다.

---

## 0. 원칙

**안전·개인정보 → 최저비용·최고레버리지 → 설계공백 클러스터(전수) → 화면별 계약 → 서버신설/메뉴결정.**
뿌리 하나가 여러 화면을 막으므로 "화면 순서"가 아니라 **레버리지·소유자 순서**로 고친다.
등급은 "사용자가 잘못 판단하게 되는가"로 정한다(예쁨 문제는 낮음).

## 1. 상태 요약

**완료 (이번 세션, 반영·배포):**
- ⑤ 작업자앱 가짜 점검항목 차단 + 실제 저장 실증
- 제출 500 근본수정(FK) · work_schedules 500 · tbm 500
- ⓐ 소유자검증 재도입 · 이메일 자가치유 · 로그 인프라 복구
- **㉑㉘㉝** 담당자배정·완료처리 저장 라우트 신설(#147)

**진행 중 (지시서 발행, 미착수):**
- **① 전송부 ㉮** — `WORKINSTRUCTION_safe-transport-error-propagation_exec_2026-08-17.md` (Cursor)
- **51·51-b** 개인정보·무인증 계약 — `WORKORDER_safe-my-diagnosis-tenant-scope_2026-08-17.md`

**파킹 (법령엔진 재작업 대기):**
- 12 점검항목 마스터(범용 16종) · 작업 전 점검 전용 템플릿 · **57 진단2 NameError(엔진 코드)**

## 2. ★ 착수 전 결정 (운영자) — 이게 캠페인의 진짜 게이트

1. **"서버가 통째로 없는" 화면 — 신설 vs 메뉴에서 내림.**
   대상: **13**(사업자등록증) · **14**(권한관리) · **17·18**(서식/PDF) · **20**(안전정보) · **27**(맞춤점검) · **36**(QR/RFID) · **64**(건설 점검항목관리 7엔드포인트) · **32**(alert, 폐기예정).
   각각 「지금 신설」/「메뉴에서 내림(문서 미작성)」 결정. 가짜 화면을 남기는 것보다 내리는 편이 안전한 경우가 많다.
2. **메뉴 위생** — 진단 하위단계(diagnosis-step1 등) 최상위 노출 여부 · 「기타」 서랍 정리.
3. **단위/어휘 정본** — 58(공사금액 억/원), 8·9·43·59·65(status/result_code/DONE/ACTIVE/PENDING). **정본을 먼저 정해야 코드+데이터를 함께 정리**한다(코드만 고치면 데이터가 남는다).

## 3. 소유자별 레인

### 레인 A — 저(Claude, MCP): 서버 라우팅·CRUD·응답봉투·파라미터·등록·DB
게이팅 없는 것부터 즉시 착수 가능.

- **응답봉투/키 정합** — 22(대시보드 미이행 카운터 `summary`→`data`) · 25/67(작업중지 기준 `criteria` 노출) · 17(서식 목록 봉투)
- **파라미터 정합** — 4(회의 date_from/to) · 29(캘린더 날짜/분류) · 23/72(알림 파라미터·`size`)
- **라우트 신설** — 3(safe 세트상세 `GET /inspection-sets/{id}/items`) · 23/72(`POST /notifications`) · 33 일부 완결
- **권한/회사 스코프 (전수 한 묶음, P13)** — 35·38·48 + /education·/quotes·/factories·legal-engine step1. **한 건씩 고치면 네 번째가 또 나온다 — 라우터 전수로.**
- **계약 필드명** — 42·43·44·49·59·60·62(건설 계열) · 2/45(교육 계열, ②와 묶음)
- **DB** — 9(status_code 전건 READY) · 47(`master_building_legal_rules` 개명) · 55(`system_codes` diagnosis_* 0건) · 45(`education_master.sort_order`) · 58(공사금액 단위 정리) · 어휘 정본 데이터

### 레인 B — GPT(엔진): 법령엔진 로직 (절대수정금지 영역, 저는 사실만 넘김)
- **57** 진단2 `run_diagnose_step2` 의 `sector_groups` 미대입 → 항상 500. **한 줄**(`sector_groups = get_sector_groups(...)` 추가 또는 반환 키 제거). 진단 흐름 전체를 막는 최우선.
- 그 밖 진단 판정 로직 계열(56 등 판정 하드코딩과 연결 시).

### 레인 C — Cursor(프런트): 화면 계약·size·전송부·판독키
- **① 전송부 ㉮** (지시서 발행) — "실패를 성공으로" 15+ 화면 일괄 소생. **레인 C 최우선.**
- **66** 작업중지 배지 — `stop.level` → `work_stop.required`/`reasons` 판독(안전 직결). 서버 계약 확정됨(`required`/`reasons`).
- **size 상한** — 24·31·54·61·68 (화면 size≤100). (또는 레인 A 서버 le 상향 택1)
- 화면 판독/필터/하드코딩 — 15·20·25·26·34·39·40·41·46·50·52·56·70·71·73

### 레인 D — 운영자 결정 (§2) → 결정 후 A/C 로 배분
서버신설 8건, 메뉴 위생, 단위/어휘 정본.

## 4. 실행 순서 권고

1. **레인 A 즉시** (게이팅 없음): 22 → 4 → 3 → 29 → 회사스코프 전수(35·38·48…). 서버 응답만 맞으면 다수 화면이 "빈 화면/거짓 성공"에서 벗어난다.
2. **레인 C 병행**: ① ㉮(별도 Goal) → 66 → size 상한.
3. **레인 B**: 57(엔진 소유자).
4. **운영자 결정(§2)** 후 서버신설(레인 A)·메뉴 위생.
5. **어휘/단위 정본**(§2-3) 확정 후 8·9·43·58·59·65 코드+데이터 동시.

각 수정 후 **라이브 관측으로 완료 판정**(모의 초록은 완료 아님). 헬프센터 후속(LEDGER §27)은 반영 통지 후 진행.

## 5. 참조
- 결함 정본: `LEDGER_safe-defects.md` (건별 번호 = 이 문서의 번호)
- 메뉴 커버리지·"뿌리 하나→여러 화면": `BASELINE_safe-menu-coverage.md`
- 완료 내역: `HANDOFF_helpcenter_2026-08-17_worker-app-and-safe-fixes.md`
- 운영 로그 접근: project `7c3ab53b…` / service `tai-api-prod 4cf52678…` / env `production 9dacb6f0…`

## 6. 기재 원칙 (LEDGER 계승)
- 한 줄이라도 실측 근거(원문/쿼리). 추측은 「미확인」으로 분리.
- 소유 경계 준수: 엔진=GPT, 프런트=Cursor, 서버·DB=저, 신설/메뉴=운영자.
- 고쳐지면 지우지 말고 〔해소〕+날짜.
