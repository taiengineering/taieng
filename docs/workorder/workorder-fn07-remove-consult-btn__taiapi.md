# FN-07: "전문가 상담" 버튼 전 페이지 삭제

**작성일**: 2026-04-18  
**작성자**: 기획창  
**선행조건**: 없음 (독립 작업)  
**적용 위치**: taiengineering/taieng → nexas/ 전체  
**배포**: main 브랜치  

---

## 배경

TAI는 상담 서비스가 아닙니다.
**데이터 입력 → 엔진 가동 → 자동 판단 → 실행** 구조입니다.

"전문가 상담 요청", "전문가 상담 신청", "상담 문의" 버튼은
TAI의 서비스 구조와 맞지 않으며, 사용자에게 잘못된 기대를 심어줍니다.

---

## TASK

### 1. 대상 파일 스캔 및 삭제

nexas/ 디렉토리 내 모든 HTML 파일에서
아래 텍스트를 포함하는 **버튼/링크 요소**를 찾아 삭제:

| 검색 키워드 | 삭제 대상 |
|---|---|
| `전문가 상담 요청` | `<a>` 또는 `<button>` 요소 전체 |
| `전문가 상담 신청` | `<a>` 또는 `<button>` 요소 전체 |
| `전문가 상담` | 버튼/링크 형태인 경우만 삭제 (본문 텍스트는 유지) |
| `상담 문의` | 버튼/링크 형태인 경우만 삭제 |

### 2. 확인된 잔존 위치 (2026-04-18 기준)

| 파일 | 위치 | 상태 |
|---|---|---|
| `for-business-owner.html` | 히어로, CTA | ✅ 이미 삭제됨 |
| `free-diagnosis-result.html` | CTA 사이드바 | ❌ **"전문가 상담 신청" 잔존** |
| `pricing.html` | 진단유도 블록 하단 | ⚠️ "직접 상담을 원하시면" 링크 잔존 |
| `pricing.html` | CTA 섹션 | ⚠️ "도입 문의하기" — 이것은 유지 가능 (상담이 아닌 문의) |
| 기타 페이지 | 미확인 | 전수 스캔 필요 |

### 3. 삭제 후 대체

| 위치 | 삭제 | 대체 |
|---|---|---|
| free-diagnosis-result.html CTA | "전문가 상담 신청" | 없음 (단일 CTA만 유지) |
| pricing.html 진단유도 하단 | "직접 상담을 원하시면" | 삭제 (진단 유도 버튼만 유지) |

### 4. 예외 (삭제하지 않는 것)

- `contact.html` 페이지 자체 — 문의 폼 페이지는 유지
- "도입 문의하기" 버튼 — 상담이 아닌 문의이므로 유지 가능
- 본문에서 "전문가"를 언급하는 경우 — 버튼이 아니면 유지

---

## 원칙

> TAI는 상담을 통해 진행되지 않는 구조입니다.
> 데이터를 입력하고, 그 데이터를 토대로 엔진이 가동하여,
> 판단하여 실행하게 만드는 구조입니다.
> — 심태왕 대표

---

## 완료 조건

- [ ] nexas/ 전체 HTML에서 "전문가 상담" 버튼/링크 전수 스캔
- [ ] 발견된 모든 상담 버튼 삭제
- [ ] 삭제 후 레이아웃 깨짐 없음 확인
- [ ] pricing.html "직접 상담을 원하시면" 삭제
- [ ] free-diagnosis-result.html "전문가 상담 신청" 삭제
- [ ] 모바일 레이아웃 확인

---

## 금기

- "전문가 상담" 텍스트가 포함된 새 버튼 생성 금지
- contact.html 링크를 "상담"으로 표현 금지
- "상담 예약", "상담 접수" 등 유사 표현 금지
