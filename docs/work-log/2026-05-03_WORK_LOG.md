# 작업일지 — 2026-05-03

## 담당자
이창은 (마케터)

## 작업 범위
`nexas/service/saas.html` — 히어로 섹션 전면 교체

---

## 완료 항목

### 1. saas.html 히어로 — saas7.png 단일 이미지 교체
- **이전 상태**: 4:6 split 히어로 (좌측 텍스트 패널 + 우측 인물진화 타임라인)
- **변경 후**: `saas7.png` 단일 이미지, 텍스트 없음
- **이미지 URL**: `https://vwlahtguyggrhvslabax.supabase.co/storage/v1/object/public/site-assets/saas7.png`
- **커밋**: `cc43b1c` feat(saas): 히어로 saas7.png 단일 이미지로 교체 (텍스트 제거)

### 2. 히어로 천장부터 시작 문제 해결 (반복 수정)
- **문제**: 히어로 이미지 위에 흰 갭이 남는 현상
- **원인 파악 과정**:
  1. `margin-top: var(--tai-nav-h, 90px)` → `margin-top: 0` 시도 → 여전히 갭
  2. CSS 파일 분석 — `tai-hero-fix.css` 에는 `.breadcrumb-area` 숨김 없음
  3. `building.html` 참조 → `tai-hero.css` 사용 중 (`.breadcrumb-area { display: none !important; }` 포함)
  4. **근본 원인**: Nexas 템플릿이 navbar 아래에 `.breadcrumb-area` 요소를 자동 삽입 → 갭 발생
- **해결 방법 (2단계 적용)**:
  - Step 1: CSS 링크를 `tai-hero-fix.css` → `tai-hero.css` 로 교체 (`fbcbe71` 커밋)
  - Step 2: 배포 캐시 우회를 위해 HTML `<style>` 블록에 인라인으로 추가 (`24aa908` 커밋)
    ```css
    .breadcrumb-area, .breadcrumb-wrap, .page-banner, .page-header { display: none !important; }
    ```
- **최종 커밋**: `24aa908` fix(saas): breadcrumb-area 인라인 숨김 직접 추가 (캐시 우회)

---

## 반복 수정 원인 분석

| 시도 | 방법 | 결과 | 이유 |
|---|---|---|---|
| 1차 | `margin-top:0` | 실패 | breadcrumb-area 갭이 남음 |
| 2차 | `navbar fixed !important` CSS | 실패 | 근본 원인 아님 |
| 3차 | `tai-hero.css` 로 교체 | 부분 성공 | Cloudflare 캐시로 미반영 |
| 4차 | 인라인 `display:none !important` | 배포 대기 중 | 캐시 우회 |

**교훈**: Nexas 템플릿에서 `.breadcrumb-area` 는 스크롤 전 항상 렌더링됨.
히어로 천장 시작이 필요한 모든 페이지는 반드시 `tai-hero.css` 를 로드하거나,
인라인 `<style>` 에 `.breadcrumb-area { display: none !important; }` 를 추가해야 함.

---

## 변경된 파일

| 파일 | 변경 내용 |
|---|---|
| `nexas/service/saas.html` | 히어로 교체 + CSS 링크 변경 + 인라인 breadcrumb 숨김 |

---

## 현재 saas.html 상태

- **히어로**: saas7.png 단일 이미지, `margin-top: 0`
- **CSS**: `tai-hero.css` 로드
- **인라인 style**: `.breadcrumb-area` 등 갭 원인 요소 `display:none !important`
- **나머지 섹션**: 겸직 타겟 멘트, 분산 구조도, 기능 목업 5개, ROI, 기능 목록, 요금제, CTA 유지
- **최종 SHA**: `66fce0ed380dac4c2aadead68f163481c9fc3904`

---

## PENDING

1. **배포 캐시 확인**: Cloudflare Pages 배포 후 `Ctrl+Shift+R` 로 실제 반영 여부 확인 필요
2. **다른 서비스 페이지 검토**: `service/` 하위 페이지들도 동일 문제 가능성 있음
