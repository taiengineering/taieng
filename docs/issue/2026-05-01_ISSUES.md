# 이슈 로그 — 2026-05-01

**레포:** `taiengineering/taieng`

---

## 🔴 미해결 이슈

### ISSUE-01: index.html 히어로 이미지 구도 미확정
**상태:** 미해결  
**파일:** `nexas/index.html`  
**현재:** `db7213a` — 2컬럼 구도 복원됨 (좌 42% 텍스트 / 우 58% 이미지)  
**문제:**
- 사용자가 "기존 이미지 사이즈를 유지하면서 구도를 잡아야" 라고 했으나
- 구체적으로 원하는 최종 결과물을 스크린샷으로 확인하지 못함
- 현재 `idx-hero-right`의 이미지는 `object-fit:cover`로 flex 컨테이너를 채움
- 이미지 원본 비율을 완전히 유지하려면 다른 접근 필요할 수 있음

**확인 필요:**
- 배포 후 실제 화면 스크린샷 수령
- 이미지가 잘리지 않고 자연스러운지 확인
- 필요 시 이미지 높이/object-position 조정

**다음 작업:**
```
사용자에게 스크린샷 요청:
"현재 index.html 히어로 화면 캡처 보내주세요.
어떤 부분이 원하시는 것과 다른지 표시해주시면 바로 수정하겠습니다."
```

---

### ISSUE-02: 루트 tai-engineering 레포 .DS_Store 추적 중
**상태:** 미해결  
**위치:** `tai-engineering` 루트 레포 (taieng 레포와 별개)
**문제:** 루트 `.DS_Store`가 Git 추적 중

**해결 방법 (Cursor 실행):**
```bash
# tai-engineering 루트 디렉토리에서:
git rm --cached .DS_Store
git commit -m "chore: 루트 .DS_Store 추적 제거"
git push
```

---

### ISSUE-03: rule_doc_mapping 작업 미완료
**상태:** 보류 (별도 세션 필요)  
**위치:** Supabase `vwlahtguyggrhvslabax` — `rule_doc_mapping` 테이블  
**현재:** 447/2583건 (17%) 완료  
**순서:** SERA(8) → OSH_R(7) → ELEC(10) → ETC(390)
**작업 방식:** Claude API 의미 매칭으로 자동 처리 필요

---

### ISSUE-04: diagnosis.html 히어로 표준 미적용
**상태:** 미해결 (의도적 보류)  
**파일:** `nexas/service/diagnosis.html`  
**현재:** `.diag-hero` 클래스 + 인라인 CSS 사용 (tai-hero 표준 미적용)
**사유:** `tai-hero-fix.css`의 `.diag-hero` 오버라이드가 있어 당장은 작동하나,
장기적으로 표준 전환 필요

**전환 시 주의:**
- 우측 이미지(`diag-hero-img`) 포함 2컬럼 구조 유지
- 기존 `padding: 80px 0 60px` / `min-height: 450px` 유지
- `.diag-hero` 이하 before 가상요소 등 유지

---

### ISSUE-05: for-agency / for-business-owner 등 미검토 페이지
**상태:** 미검토  
**대상:**
- `nexas/for-agency.html` — fa2-* 커스텀 (히어로 작동은 하나 CSS 링크 표준화 여부 불명)
- `nexas/for-business-owner.html` — 히어로 구조 미확인
- `nexas/about.html` — 히어로 구조 미확인
- `nexas/pricing.html` — 히어로 구조 미확인
- `nexas/connect.html` — 히어로 구조 미확인

**다음 작업:** 각 파일 열어 히어로 클래스 확인 후 TASK_HERO_STANDARDIZE.md 기준 적용

---

## 🟡 주의 사항

### NOTE-01: tai-brand.css의 !important 오버라이드 한계
- header.js가 JS로 `<style>`을 동적 인젝션하므로 CSS 파일보다 항상 나중에 적용됨
- tai-brand.css의 `!important`가 header.js의 `!important`에 질 수 있음
- **결론:** header.js 색상 수정은 반드시 header.js에서 직접 해야 함

### NOTE-02: Nexas 템플릿 잔재 .breadcrumb-area
- `tai-hero.css`에 `.breadcrumb-area { display:none !important }` 글로벌 숨김 추가됨
- 단, 물리적 HTML 삭제가 더 깨끗함
- `target/construction.html`은 이미 교체 완료
- 나머지 페이지는 TASK_HERO_STANDARDIZE.md 참고

### NOTE-03: index.html CSS 구조 변경
- `tai-hero-fix.css` 링크 제거됨 (db7213a)
- `tai-main.css` + `tai-hero.css` 링크 추가됨
- `.hero-split` 클래스 → `.idx-hero`로 교체됨 (이전 CSS 규칙 참조 주의)

---

## ✅ 해결 완료

- [x] navbar-area-fixed 텍스트 안 보이는 버그 (61a1d8a)
- [x] tai-hero.css 표준 모듈 생성 (e84012a)
- [x] Cursor 히어로 표준화 작업지시서 생성 (2bcad76)
- [x] nexas/assets/.DS_Store 추적 제거 (a9b8959)
- [x] construction/factory/building/for-expert/for-safety-manager/for-consultant/for-repair 히어로 정리
- [x] diag-hero padding-top 80px, min-height 450px 확정
