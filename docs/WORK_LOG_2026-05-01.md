# 작업 로그 — 2026-05-01

**레포:** `taiengineering/taieng` (`nexas/` 디렉토리)
**배포:** Cloudflare Pages → taieng.co.kr

---

## 커밋 순서 (시간 순)

### 1. `c2b40e7` — diag-hero 높이 조정
- 파일: `nexas/assets/css/tai-hero-fix.css`
- `.diag-hero` padding-top: `80px`, min-height: `450px`

### 2. `ebca4c8` — navbar-area-fixed 텍스트 흰색 오버라이드 (tai-brand.css)
- 파일: `nexas/assets/css/tai-brand.css` v2.3
- 고정 헤더 배경이 다크 네이비인데 텍스트도 네이비여서 안 보이는 버그 발견
- `tai-brand.css`에 오버라이드 추가 시도 → CSS 캐스케이드 순서 문제로 미적용

### 3. `61a1d8a` — header.js v3.5.2 텍스트/버튼 색상 수정 (근본 수정)
- 파일: `nexas/assets/js/header.js`
- `.navbar-area-fixed` 상태: 로고텍스트·아웃라인버튼·로그아웃 `#0f2b4a`(네이비) → `rgba(255,255,255,.9)`(흰색)
- 솔리드버튼(회원가입): 다크 배경+흰글 → **흰 배경+네이비글**
- 원인: `tai-brand.css v2.1`에서 fixed 배경을 다크 네이비로 바꿨으나 header.js 텍스트색은 갱신 안 됨

### 4. `e84012a` — tai-hero.css 표준 모듈 신규 생성
- 파일: `nexas/assets/css/tai-hero.css` (신규)
- 표준 클래스: `.tai-hero`, `.tai-hero--dark`, `.tai-hero--image`
- `.tai-hero__bg` / `.tai-hero__overlay--left` / `.tai-hero__inner`
- 핵심: `<section>` 자체는 `padding:0` (탑부터 시작), 내부 `__inner`에 `padding-top: calc(var(--tai-nav-h,90px)+40px)`
- `.breadcrumb-area { display:none !important }` 포함 (Nexas 잔재 글로벌 숨김)

### 5. `2bcad76` — Cursor 작업지시서 생성
- 파일: `docs/TASK_HERO_STANDARDIZE.md`
- 7개 파일 대상, TYPE-A(breadcrumb 교체) / TYPE-B(클래스명 통일) / TYPE-C(CSS링크만) 분류
- **절대 규칙**: 히어로 섹션(탑)만, 하단 콘텐츠 절대 손대지 않음

### 6. `a9b8959` — .DS_Store 추적 제거 (Cursor 실행)
- `git rm --cached nexas/assets/.DS_Store`
- 루트 tai-engineering 레포의 `.DS_Store`는 별도 레포에서 동일 처리 필요

### 7. Cursor 작업 완료 보고 (커밋 별도)
- `target/construction.html` — A안: `breadcrumb-area` → `tai-hero` 교체
- `target/factory.html` — B안: 클래스명 통일
- `target/building.html` — B안: 클래스명 통일
- `for-expert.html` — **B안**: `fe2-a` 커스텀, CSS 링크만 정리
- `for-safety-manager.html` — **B안**: `fs2-*` 커스텀, CSS 링크만
- `for-consultant.html` — **B안**: `fc2-*` 커스텀, CSS 링크만
- `for-repair.html` — **B안**: `fr2-*` 커스텀, CSS 링크만

### 8. `5c09d90` — index.html 히어로 1차 수정 (전체 배경형)
- `hero-split` → `tai-hero--image` 전체 배경으로 변환
- 사용자 피드백: "기존 이미지 사이즈 유지하면서 구도를 잡아야"  → 롤백 방향

### 9. `db7213a` — index.html 히어로 2차 수정 (2컬럼 구도 복원)
- `.idx-hero` flex 2컬럼 (좌 42% 텍스트 / 우 58% 이미지) 복원
- 섹션 y=0 시작: 이미지가 네비 뒤까지 채워짐
- 좌측: `padding-top: calc(var(--tai-nav-h,90px)+44px)` → 텍스트 네비 아래 배치
- 이미지 크기·구도·filter 원본 유지, 배지 유지
- `tai-hero-fix.css` 제거 → `tai-main.css` + `tai-hero.css` 교체

---

## CSS 파일 현재 구조

```
nexas/assets/css/
  style.css          — Nexas 템플릿 base (건드리지 않음)
  responsive.css     — Nexas responsive (건드리지 않음)
  tai-brand.css      — TAI 색상 변수 + navbar 오버라이드 v2.3
  tai-main.css       — TAI 공통 컴포넌트
  tai-hero.css       — [NEW] 표준 히어로 단일 모듈 v1.0.0
  tai-hero-fix.css   — 레거시 (.diag-hero 오버라이드만, diagnosis.html에만 유지)
  tai-auth.css       — 로그인/인증 페이지
  tai-mypage.css     — 마이페이지
```

**표준 `<head>` CSS 링크 순서:**
```html
style.css → responsive.css → tai-brand.css → tai-main.css → tai-hero.css
```

---

## 결정 사항 (이번 세션)

- `for-agency.html`의 `fa2-a` 등 완전 커스텀 섹션: 히어로 HTML 건드리지 않음 (B안)
- `tai-hero-fix.css`는 `diagnosis.html`에만 남기고 다른 페이지에서 제거
- Cursor 작업지시서 작성 원칙: 히어로(탑)만, 하단 절대 건드리지 않음, 판단 필요시 보고
