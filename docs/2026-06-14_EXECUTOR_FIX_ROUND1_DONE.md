# 2026-06-14 executor 보정 1회차 완료 — 진척 기록 (다음 세션 이어받기)

> 함께 읽기: 2026-06-14_EXECUTOR_FIX_STRATEGY.md(전략·완벽 정의),
> 2026-06-13_SESSION_END_EXECUTOR_FIX_STATUS.md, _ROOTCAUSE_PIPELINE_SWAP.md(9·10장)

---

## 오늘(6/14) 한 일 — 갈래1 보정 1회차 (임시테이블만)

전략 "검증가능한 것만 깎아내려가기"의 첫 회차 실행:
1. H: 갈래1 7,065건의 원문 맨앞주어 Kiwi 추출 → PERFECT 후보 선별
   - 표본 검증서 "A의 B"(위원회의 위원→위원회만) + 맨앞사물 오차 발견
2. I: 1회차 대상을 "명사+주격조사 직결"로 좁힘 → PERFECT_V2 1,555건
   - executor_fixed 채움 → Kiwi 재색출(조건1 검증): REFIX_FAIL 1건(적재함=사물)
3. 반영 기준 한 번 더 조임(대표): **사전 등재 1,280건만** (사물 주어 차단)
   - 적재함·산불 등 사전 미등재 사물은 자동 배제
4. 임시테이블 반영: semantic_clause_fix 1,280건 executor_text ← executor_fixed, status='APPLIED'
5. 원본 비교분석 통과:
   - 달라진 행 정확히 1,280 / 전부 APPLIED / 전부 갈래1 / 원본값 박제 보존 / source_text 변경 0
   - 산안법 17조 executor: 사업주(임시) vs 지도ㆍ조언하(원본 미변경) — 정상

## ★ 핵심 성과
산안법 17·18·19조 "두어야 한다"(안전·보건관리자·담당자 선임) executor:
  지도ㆍ조언하(틀림) → **사업주**(보정). 제조업 진단 선임 누락의 직접 해결점.

## 현재 semantic_clause_fix(임시테이블) 상태
| 구분 | 건수 | 상태 |
|---|---|---|
| 갈래1 사전등재 주격직결 | 1,280 | ✅ APPLIED (executor_text 보정됨) |
| 갈래1 잔여 | 5,785 | 미보정 (소유격806/기타조사736/조사없음3,767/사전미등재·사물 등) |
| 갈래2 수범자 동사구 | 4,464 | 미보정 (별도 전략 필요) |
| 비주어 | 408 | 미보정 (깨진추출/사물) |
| OK 정상주어 | 29,519 | 정상 |
- executor_original에 전건 원래값 박제(되돌리기 가능).
- **원본 semantic_clause·운영 미변경.** (대표 결정: 보정 다 끝낸 뒤 원본 한 번에 복사 = 2번)

## 다음 회차 (남은 보정, 임시테이블에서 계속 깎기)
1. **갈래1 잔여 — 소유격 "A의 B"**: 806건. "의" 뒤 명사(위원/대표자/장)까지 추출 규칙 보강.
   단 "A의 B의 C" 중첩 주의. 표본 검증 필수.
2. **갈래1 잔여 — 사물 주어 차단 규칙**: 적재함/산불 등. 사전 미등재 + 사물접미(설비/장치/함 등)
   → 보정 안 함, needs_review 또는 content_type 재검토(피동규정문 가능).
3. **갈래2 — 수범자 동사구**(4,464): "받으려/하려"=" ~하려는/받는 자"가 주어. 맨앞주어·조문빌리기
   둘 다 부적합. 별도 전략(원문에서 "~하려는 자/받은 자" 복원, 또는 needs_review).
4. **못 깎는 소수 → LLM**: 위 규칙들로도 안 되는 잔여. 수 작아진 뒤, 원문 대조 검증,
   코어 아닌 별도 단계 격리(생성/분해 배제 유지).
5. 보정 충분히 안정 후 → **원본 semantic_clause 복사**(대표 승인). 그 후 D단계(엔진 경로 전환).

## 원칙 (불변)
- 분해기·Kiwi사전 무변경. 동사어간 사전등록 금지. 원본은 마지막에 한 번.
- 매 회차 "완벽 2조건"(재검증 작동 ∧ 원문서 확정) 만족분만. 표본 글읽기 후 확대.
- executor 한 단어가 수십만명 지시/패싱 좌우 → 틀린 보정보다 미룸이 안전.

## 미결 (대표 처리)
- 환경변수 노출 → 키 rotate 권고(결제·LLM·DB·세션).
- leg 레포 핸드오프(45cminc/leg eeeff59)를 taieng로 모을지(현재 동선 밖).
