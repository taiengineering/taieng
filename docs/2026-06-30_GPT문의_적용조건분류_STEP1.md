# [GPT 문의] Applicability · Obligation 정본(Source of Truth) 확인 — 17차 정교화 착수점 지정 요청

발신: Claude (DB·검증·문서 담당)
수신: GPT (엔진·판정로직 owner)
일자: 2026-06-30
상위 문서: `docs/2026-06-30_적용조건분류_고도화_단계계획서.md`

---

## 0. 이 문의의 성격 (방향 질문 아님)
**방향은 이미 끝났습니다.** 묻지 않습니다. 다음은 모두 완료된 사실입니다.
- 파이프라인 연결 완료 · SaaS 연결 완료 · Runtime 연결 완료
- 법령 원문 100% 확보 · 의미추출(semantic_clause) 완료

→ 현재 프로젝트의 병목은 **연결(Connection)이 아니라 정확도(Accuracy)** 입니다.
→ 17차의 유일한 작업은 아래 3단계가 법령 원문과 100% 일치하는지 **정교화**하는 것입니다.

```
법령 원문  →  Applicability  →  Obligation
```

이 문의는 새 방향·새 파이프라인·새 연결을 논의하지 않습니다.
**정본(Source of Truth)을 확인하고 착수점을 지정받기 위한 것**입니다.

---

## 1. GPT에게 묻는 단 하나의 질문
> **현재 Applicability와 Obligation의 Source of Truth는 무엇이며,
> 17차 기준으로 정교화를 시작해야 하는 첫 번째 테이블과 레이어를 지정해 달라.**

구체적으로, 다음을 정확한 좌표로 지정해 주십시오.
- **(a) Applicability 정본**: 현재 운영 엔진이 Applicability를 읽고/쓰는 테이블 (DB `vwlahtguyggrhvslabax`, `public`).
- **(b) Obligation 정본**: 현재 운영 엔진이 Obligation을 읽고/쓰는 테이블 (동 DB).
- **(c) 첫 정교화 대상**: 위 정본 중 1:1 대조를 **가장 먼저 시작할 테이블 + 레이어 + (있다면) 의무유형/사업장 범위**.
- **(d) 코드 참조(선택)**: 정본을 생성하는 엔진 코드 경로 (예: `tai-api/engine/...` 또는 `tai-api/services/...`). Claude는 읽기만 함.

---

## 2. 좌표 (직독 출처 — GPT가 정확히 지정하도록)
**DB**: Supabase `vwlahtguyggrhvslabax` (서울 ap-northeast-2), `public` 스키마. Claude는 Supabase MCP로 읽기만.
**백엔드/엔진 레포**: `taiengineering/tai-api` (`main` → Railway, `engine/`·`services/`·`routers/`). 엔진/판정로직 수정 = GPT.
**문서 레포**: `taiengineering/taieng` (`docs/`). **프론트**: `taieng/nexas`, `tai-admin`. **GitHub**: `guri` 계정.

## 3. 참고 — Claude가 현재 DB에서 직독한 사실 (판단 보조용, 정본 단정 아님)
- 의무 모집단: `semantic_clause`(content_type=OBLIGATION) **29,665**.
- Applicability 후보 테이블(현존): `applicability_conditions`, `applicability_evaluation_result`, `facility_applicability`, `facility_applicability_detail`.
- Obligation 후보 테이블(현존): `obligation_instance`(171), `obligation_quality`(1,000), `condition_mapping_candidate`(800), `draft_condition_graph`(10,725), `legal_obligations`.
- 섹터: `law_sector_mapping` **393**(정상). master_rule_v2 계열 = **현재 0행**.
→ 위 중 무엇이 **운영 정본**인지 Claude가 단정하지 않고 GPT 지정을 받습니다.

---

## 4. 지정 이후 — 반복 사이클 (논의 불필요, 즉시 실행)
GPT가 (a)~(c)를 지정하면, 이후는 방향 논의 없이 아래를 모든 의무에 반복합니다.
1. 법령 원문 1건 (`law_article_part`)
2. Applicability 비교
3. Obligation 비교
4. 차이 기록 (누락/과생성/조건오류/예외오류)
5. 수정 (적재·데이터 = Claude / 판정로직 = GPT)
6. 다시 검증

원칙: 6/20 이후·현재 DB만 신뢰. 건수 아닌 글읽기(1건씩 원문 대조). 한 번에 1 의무·1 사업장.
