# 2026-06-14 executor 보정 전체 1차 완료 — 최종 진척 (다음 세션 이어받기)

> 함께 읽기: _EXECUTOR_FIX_STRATEGY.md(완벽2조건·BLOCKLIST), _LEGAL_PIPELINE_3LAYER.md(3층),
> _LLM_EXECUTOR_RULES_VALIDATION.md(LLM 규정·4중그물). 모두 taiengineering/taieng/docs/.

---

## 전체 결과 (오류 12,358건 전수 분류·처리, 임시테이블 semantic_clause_fix)

| 구분 | 건수 | 비율 |
|---|---|---|
| 보정 완료 (APPLIED 계열) | 5,342 | 43% |
| 제외·의무아님 (벌칙/위임/효력) | 741 | 6% |
| 미보정 잔여 (3층 LLM 코드화 대상) | 6,275 | 51% |
- 원본 대비 executor 변경 5,337 / **source_text 변경 0**(원문 무손상). executor_original 박제(되돌리기).
- 원본 semantic_clause·운영 **미반영**(대표 결정: 보정 안정 후 한 번 복사 → D단계).

## 보정 회차별 (APPLIED status)
| status | 내용 | 건수 |
|---|---|---|
| APPLIED | 갈래1 주격직결 사전등재 | 1,280 |
| APPLIED_G2A | 갈래2 수식절 뒤 주어명사(받으려는 자 등) | 2,425 |
| APPLIED_JANG | 갈래1 "~의 장" | 443 |
| APPLIED_HEAD | 잔여 맨앞 사전등재 실행주체(일괄) | 343 |
| APPLIED_B | 갈래2 금지문(누구든지 + 맨앞주체) | 209 |
| APPLIED_ENUM | 갈래1 나열 복합주어 | 204 |
| APPLIED_DICT | 갈래1 사전미등재 정당주체 | 168 |
| APPLIED_DEEP | 잔여 맨앞 사전등재/주체접미 주격직결 | 140 |
| APPLIED_POSS | 갈래1 소유격 "A의 B" | 60 |
| APPLIED_LLM_C | 갈래2 C부사구(LLM 3층, 4중그물) | 48 |
| **합계** | | **5,342** |

## ★ 이번 마지막 회차의 교훈 (중요)
- "문장 어딘가의 첫 주격 주어"를 잡으려 시도 → 1,421건 중 1,280건 오추출(중간주격 "통지받은 자는…
  산업통상부장관이 정하는"에서 산업통상부장관 잡음, 사물 회의/검사항목/공장재단, 수식절 자격이있는).
  표본 글읽기로 발견 → **전량 롤백.** 맨앞 주격직결만 남겨 140건.
- **재확인된 철칙: "맨 앞 주격직결"만 안전. "문장 중간 주격"은 위험(중간주격·사물·수식절 대량 혼입).**
  단순 SQL 규칙으로 깎을 수 있는 한계 도달. 남은 6,275는 진짜 3층 LLM(문장구조 이해) 영역.

## 제외 (의무아님 — content_type 재검토 1층 영역) 741
- EXCLUDE_PENALTY 411(벌칙 "해당하는 자는…처한다/과태료"), EXCLUDE_NONOBLIG 약 328(위임/준용/효력),
  BLOCKED_NONSUBJECT 2(대통령령). **executor 문제 아니라 content_type 오분류 → 1층 GPT 작업지시.**

## 미보정 잔여 6,275 (DEFER_REVIEW/DEFER_ENUM) = 3층 LLM 코드화 대상
- 성격: 조건절/수식절 뒤 깊은 주어("통지를 받은 자는"=자가 주어인데 맨앞은 "통지를"), 문장중간
  주격 혼재, 생략, 사물. **맨앞 주격직결 규칙으로 더 안 깎임.**
- 처리 경로: 단순 SQL 규칙 ❌ → **코드화된 LLM 파이프라인**(배치 LLM 호출 + 4중그물). C부사구 261을
  이 창서 직접 푼 방식의 코드화 버전. 6,275를 창에서 한건씩은 비현실적 → 코드화 단계서 일괄.
- = "끝에 LLM으로 갈 어려운 것"의 실체. 2·3층 코드화 시 입력.

## ★ LLM 3층 검증방식 유효성 (이번 세션 증명)
- C부사구 261건을 Claude(=LLM)가 직접 풂. 1차 채택후보 137 → 4중그물(특히 그물4 표본글읽기)로
  사물·수식절(B2)·동사조각·복합주어잘림 89건 걸러냄 → 맨앞 사전등재 실행주체 48건만 채택.
- **LLM이 "푼" 게 아니라 LLM 후보를 검증그물이 걸러 48만 살아남음.** 검증방식 유효성 증명.
- 마지막 회차 1,280 롤백도 같은 원리: 표본 글읽기(그물4)가 대량 오염을 막음.

## 핵심 성과
- 산안법 17·18·19조 선임 executor: 지도ㆍ조언하(틀림)→사업주. 제조업 진단 선임누락 직접 해결.
- 매 회차 표본 글읽기로 함정 발견·규칙화·롤백: A의B / 사물 / 수식절주어(B2) / 나열 / 사물~등 /
  벌칙 / 수령대상 / 동사조각 / 복합주어잘림 / 중간주격. **틀린 보정 0**(표본이 매번 사고 차단).

## 다음 (D단계로 가는 길)
1. content_type 오분류(벌칙·사물피동 OBLIGATION) 741 → **1층 GPT 작업지시**(분해기 영역, 직접수정 금지).
2. 2·3층 보정 규칙 **코드화**(맨앞 사전등재 주격 / 소유격 / 의장 / 나열 / 수식절뒤주어 / 금지문 /
   BLOCKLIST B1·B2·B3 / 4중그물 LLM) → 새 법 자동적용 소스. = 분해기 뒤 보정 파이프라인 스크립트.
   잔여 6,275는 이 파이프라인의 LLM 단계 입력.
3. 코드화·검증 안정 후 → 원본 semantic_clause 복사(대표 승인) → **D단계: 엔진이 의미절 읽도록 경로전환**.
   선결: sectors[] 복원, 의미절 직접읽기 vs master_rule_v2 변환 결정.

## 원칙 (불변)
- 분해기·Kiwi사전 무변경. 원본 마지막 한 번. 매 회차 표본 글읽기 후 확대.
- **맨앞 주격직결만 안전, 문장중간 주격은 위험.** LLM은 발췌·분석에만, 4중그물+표본 통과해야 채택.
- executor 한 단어가 수십만명 지시/패싱 좌우 → 틀린 보정보다 미룸.

## 미결 (대표 처리)
- 환경변수 노출 → 키 rotate 권고(결제·LLM·DB·세션).
- leg 레포 핸드오프(45cminc/leg)를 taieng로 모을지.
