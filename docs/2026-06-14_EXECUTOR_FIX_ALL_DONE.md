# 2026-06-14 executor 보정 전체 1차 완료 — 최종 진척 (다음 세션 이어받기)

> 함께 읽기: _EXECUTOR_FIX_STRATEGY.md(완벽2조건·BLOCKLIST), _LEGAL_PIPELINE_3LAYER.md(3층),
> _LLM_EXECUTOR_RULES_VALIDATION.md(LLM 규정·4중그물). 모두 taiengineering/taieng/docs/.

---

## 전체 결과 (오류 12,358건 전수 분류·처리, 임시테이블 semantic_clause_fix)

| 구분 | 건수 | 비율 |
|---|---|---|
| 보정 완료 (APPLIED 계열) | 5,203 | 42% |
| 제외·의무아님 (벌칙/위임/효력) | 741 | 6% |
| 사람검토 잔여 (needs_review) | 6,414 | 52% |
- 원본 대비 executor 변경 5,199 / **source_text 변경 0**(원문 무손상). executor_original 박제(되돌리기).
- 원본 semantic_clause·운영 **미반영**(대표 결정: 보정 안정 후 한 번 복사 → D단계).

## 보정 회차별 (APPLIED status)
| status | 내용 | 건수 |
|---|---|---|
| APPLIED | 갈래1 주격직결 사전등재 | 1,280 |
| APPLIED_G2A | 갈래2 수식절 뒤 주어명사(받으려는 자 등) | 2,425 |
| APPLIED_JANG | 갈래1 "~의 장" | 443 |
| APPLIED_HEAD | 잔여 전체 맨앞 사전등재 실행주체(최종 일괄) | 343 |
| APPLIED_B | 갈래2 금지문(누구든지 + 맨앞주체) | 209 |
| APPLIED_ENUM | 갈래1 나열 복합주어 | 204 |
| APPLIED_DICT | 갈래1 사전미등재 정당주체 | 168 |
| APPLIED_POSS | 갈래1 소유격 "A의 B" | 60 |
| APPLIED_LLM_C | 갈래2 C부사구(LLM 3층, 4중그물 통과) | 48 |
| **합계** | | **5,203** (+APPLIED_HEAD에 NULLEXEC/비주어 일부 흡수) |

## 제외 (의무아님 — content_type 재검토 1층 영역)
- EXCLUDE_PENALTY 411(벌칙 "해당하는 자는…처한다/과태료"), EXCLUDE_NONOBLIG 약 330(위임/준용/효력),
  BLOCKED_NONSUBJECT 2(대통령령 법령형식어). 합 741.
- **이들은 executor 문제 아니라 content_type(OBLIGATION 오분류) 문제 → 1층 GPT 작업지시 대상.**

## 사람검토 잔여 6,414 (DEFER_REVIEW/DEFER_ENUM)
- 맨앞 주격 없음/조건절·수식절 깊은 주어/생략/사물/나열 조문참조 등.
- 규칙·LLM-C로 못 깎은 진짜 어려운 잔여. 사람(대표/Claude) 검토 또는 보류.
- 다음 세션 후보: 이 중 패턴 더 있는지 표본 글읽기 → 추가 규칙화 또는 LLM 확대.

## ★ LLM 3층 검증방식 유효성 (이번 세션 증명)
- C부사구 261건을 Claude(=LLM)가 직접 풂. 1차 채택후보 137 → 4중그물(특히 그물4 표본글읽기)로
  사물·수식절(B2)·동사조각·복합주어잘림 89건 걸러냄 → 맨앞 사전등재 실행주체 48건만 채택.
- **결론: LLM이 "푼" 게 아니라 LLM 후보를 검증그물이 걸러 48만 살아남음.** 137 그냥 믿었으면
  틀린주어 89개 유입될 뻔. 검증그물이 실제 작동 = 검증방식 유효성 증명.
- 부수: C부사구 LLM 보정도 결국 갈래1 규칙(맨앞 사전등재 주격+B2 제외)의 재적용. "LLM만의
  판단" 거의 불필요, 검증그물이 채택 결정. → 2·3층 코드화 시 동일 규칙으로 통합 가능.

## 핵심 성과
- 산안법 17·18·19조 선임 executor: 지도ㆍ조언하(틀림)→사업주. 제조업 진단 선임누락 직접 해결.
- 매 회차/유형 표본 글읽기로 함정 발견·규칙화: A의B / 사물 / 수식절주어(B2) / 나열 / 사물~등 /
  벌칙 / 수령대상 / 동사조각 / 복합주어잘림. 틀린 보정 0(표본이 매번 사고 차단).

## 다음 (D단계로 가는 길)
1. 사람검토 잔여 6,414 중 추가 패턴 표본 글읽기 → 규칙·LLM 확대(선택).
2. content_type 오분류(벌칙·사물피동 OBLIGATION) 741 → **1층 GPT 작업지시**(분해기 영역, 직접수정 금지).
3. 2·3층 보정 규칙 **코드화**(맨앞 사전등재 주격 / 소유격 / 의장 / 나열 / 수식절뒤주어 / 금지문 /
   BLOCKLIST B1·B2·B3 / 4중그물) → 새 법 자동적용 소스. = 분해기 뒤 보정 파이프라인 스크립트.
4. 코드화·검증 안정 후 → 원본 semantic_clause 복사(대표 승인) → **D단계: 엔진이 의미절 읽도록 경로전환**.
   선결: sectors[] 복원, 의미절 직접읽기 vs master_rule_v2 변환 결정.

## 원칙 (불변)
- 분해기·Kiwi사전 무변경. 원본 마지막 한 번. 매 회차 표본 글읽기 후 확대.
- LLM은 발췌·분석에만(생성/분해 배제). LLM 답도 4중그물+표본 통과해야 채택.
- executor 한 단어가 수십만명 지시/패싱 좌우 → 틀린 보정보다 미룸.

## 미결 (대표 처리)
- 환경변수 노출 → 키 rotate 권고(결제·LLM·DB·세션).
- leg 레포 핸드오프(45cminc/leg)를 taieng로 모을지.
